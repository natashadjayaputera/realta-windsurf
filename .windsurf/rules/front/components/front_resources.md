---
trigger: model_decision
description: "Use in ToCSharpFront workflow to Hardcoded string and labels must be moved to {ProgramName}FrontResources project"
---

MANDATORY: All hardcoded string and labels in `.razor` and `.razor.cs` must be moved to {ProgramName}FrotnResources project